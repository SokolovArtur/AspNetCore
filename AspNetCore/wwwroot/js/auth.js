/* Write your Javascript code. */

$(document).ready(function () {
    var form = $("#auth-template");

    form.css({
        opacity: 1,
        "transform": "scale(1)",
        "-webkit-transform": "scale(1)",
        "transition": ".5s",
        "-webkit-transition": ".5s"
    });
});
